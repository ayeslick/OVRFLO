import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create — OVRFLO",
  description: "Choose a Self-Repaying Loan or a Fixed Return.",
};

export default function CreateLayout({ children }: { children: React.ReactNode }) {
  return children;
}