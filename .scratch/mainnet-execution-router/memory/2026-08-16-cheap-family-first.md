Do not ban GLM for one empty review. Ticket 11's glm-5.2-high review died after "Let"
with no verdict (2026-08-16). That is a retry, not a quality signal. GLM is eligible,
not mandatory — do not hard-pair "Grok implemented → GLM must review."

GPT/Anthropic cost more. They are not the default. Require `typecheck` on web tickets
rather than paying a dearer model to notice Vitest-without-tsc. Escalate to GPT/Opus
when cheap reviews died twice or a money-path claim is still unreviewed.
