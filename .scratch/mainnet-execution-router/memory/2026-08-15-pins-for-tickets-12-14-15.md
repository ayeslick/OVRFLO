Binding pins harvested from the retired sweep track's lens reports — carry into the dispatch prompts of tickets 12, 14, 15. Verified with file:line by the lens agents; re-verify on apply.

## Ticket 12 (previewBorrow web half) — from the 007 lens pass

- PLAN ERROR to route around: 007 claims the five mirrored lending-math functions have one
  consumer. False: web/lib/positions.ts imports upfrontBps which calls factor. Pin: handle
  upfrontBps explicitly (inline its arithmetic or delete the teaser) — do not leave factor
  exported, do not break positions.ts.
- PLAN ERROR: web/lib/actions/types.ts BorrowQuoteState carries grossPrice/netToBorrower,
  consumed by web/lib/actions/borrow.ts (:141-146,:211-212), live-action-plan.ts (:660,662),
  e2e fixtures. Pin: replace with actualBorrow/feeAmount (net derived = actualBorrow - feeAmount)
  and update all consumers. NOTE: ticket 01 proved actions/borrow.ts is LIVE (registry ->
  buildAction -> useWriteFlow); its rework happens here in ticket 12, not as a deletion.
- Query key: add borrowKeys.quote to web/lib/query-keys.ts following its tuple conventions
  (addr()/id() normalization), NOT an object key. enabled:false when targetBorrow==0n, no tick,
  or no stream selected (previewBorrow reverts ZeroTarget on 0).
- MAX cap: BorrowFlow currently computes cap synchronously (streamDerivedCap at :236-239) and
  feeds amountFieldError. Pin one architecture: a background MAX query
  (previewBorrow(..., uint128.max)) keyed per tick selection populates cap; input validation uses
  it when present, no false rejections while in flight.
- Canonical snapshot type: consolidate into web/components/borrow/quote.ts (re-export from
  actions/types.ts): { block:{N,H}, actualBorrow, feeAmount, net, obligation, streamRemaining,
  residual, aprBps, target, minAcceptable }.
- quoteDrift compares actualBorrow/feeAmount/obligation only — never block number.
- EmptyTick revert from previewBorrow maps to {emptyTick:true, zeros}, driving the existing
  empty-tick UI state — not an unhandled query error.
- Stale display: placeholderData keepPreviousData; isStale = isFetching || isDebouncing;
  data-stale styling; dashes only before the first quote ever resolves.
- Debounce: mechanism pinned (debounce amountRaw; immediate on tick/stream change, flushing the
  timer); duration is a named constant (~250ms), not scattered literals.
- Error parity: previewBorrow shares all pre-fill reverts with borrow but can NEVER revert
  BelowMinAcceptable (no minAcceptable arg). Decode test: hex-fixture unit test in
  web/tests/lib/, not an anvil E2E.

## Ticket 11 (lens bytecode + protocol client) — from the ticket-09 review (approved 2026-08-15)

- ABI HANDOFF: generate the lens ABI/bytecode from the CONTRACT (src/OVRFLOStreamLens.sol), never
  from plan 005's Interface block — the plan block is stale (`withdrawable` vs the real
  `withdrawableAmount`, and it omits isDepleted/wasCanceled before ok).
- Deployless call MUST be viem's deploy-then-call recipe: call({ code: CREATION bytecode, data:
  encoded batch selector }). Do not invent a constructor that returns StreamView[] — the lens has
  no such constructor and raw initcode return is runtime bytecode, not decodable rows.
- OOG behavior verified: a gas-starved eth_call fails loudly (cannot encode the return), it does
  not produce a page of ok:false rows. The client can treat transport/OOG errors as unavailable
  without fearing silent degradation.
- wasCanceled can in principle remain true after a later depletion in upstream Sablier; OVRFLO
  mints non-cancelable streams so the path is dormant — do not build UI logic assuming
  wasCanceled and isDepleted are exclusive.

## Ticket 14 (watch pager + pin) — from the 001 lens pass

- Query key must contain chainId, lockup address, account, pin.blockHash (lowercased) or
  invalidate.ts regex matching (:59-65,:163-173) silently stops invalidating.
- pageParam: scalar bigint from 0n; getNextPageParam returns last+PAGE_SIZE while < sourceCount
  else undefined; sourceCount==0 short-circuits without RPC (tokensOfOwnerIn reverts on
  start>=stop).
- Auto-fetch on all-ineligible pages: sequential while renderCount==0 && hasNextPage &&
  !isFetchingNextPage, CAPPED (~4 auto pages), then show LOAD MORE.
- Four fields live ON the book data type: {streams, sourceCount, renderCount, complete,
  confirmedEmpty} — not metadata.
- Mid-pagination failure: unavailableOutcome with transport failure, data kept for useLastKnown
  degraded painting — never ready-with-partial.
- Thread TanStack context.signal through loadStreamPage into client.call.
- Lender book: loansOf hydration moves into loadLenderPage in the protocol client (no useQueries
  across dynamic infinite pages).
- CONSUMER MIGRATIONS (blast radius the plan under-lists): watch-entry.ts EntryBook gains the
  four fields (confirmedEmpty gates first-run; renderCount>0 gates hydrated); WatchApp toBook
  (:91-93) maps them; wallSurface (:206) = confirmedEmpty ? empty : (renderCount==0 && !complete)
  ? loading : ready; Wall lensTab (:283) hides only on confirmedEmpty; selectedStream /
  showStreamClosed (:174-176,:215-226) requires complete before declaring STREAM CLOSED;
  BorrowFlow uses the complete-set read, never the windowed pager; claim-all input sourced
  exclusively from loadCompleteStreams; useLoanStreams stays scoped to loaded loan rows; update
  the shared book mocks (tests/inventory/watch-app-mocks.tsx etc.) with one mockBookOutcome
  helper.
- Duplicate id across pages: unavailable with incomplete failure — a test must pin that no Set
  dedup sneaks in.
- LOAD MORE: stable selector (data-ui), disabled while fetching, last element in the tabpanel;
  keep focus on the button, aria-live polite announcement.

## Ticket 15 (004 E2E) — from the 004 reconciliation scan

- 004's text is stale in 9 places (header scope, Vitest rationale, MAX_ENUMERATION_IDS renaming,
  4-reads-per-id cost table, missing source-coordinate rule, RPC-style Gherkin steps, file
  manifest, pin-verification mechanics, reorg declared out of scope). Ticket 15's worker treats
  the CURRENT decision state as authoritative and 004's stale text as void — 008 mandates E2E
  reorg fault injection despite 004's out-of-scope line.
- Scenarios to land: reorg mid-enumeration (page 1 at N, anvil reorg, page 2 with old hash +
  requireCanonical -> unavailable -> refresh recovers); pledged-stream degraded-not-vanished
  (owner book ready + borrower book failed); provider failure mid-snapshot -> restart page 1 on
  fallback; resume at first unconsumed source index; all-ineligible window auto-advances; DOUBLE
  swap-and-pop (transfer indices 0 and 1) across a page boundary; single ok:false row degrades
  the row not the page; duplicate-id -> unavailable banner; confirmedEmpty timing (no first-run
  routing before the last page).
- Simulation-at-latest is already 003's decision — cross-reference, not a new scenario decision.
- Fixture mechanics: page-size override via env (NEXT_PUBLIC_STREAM_PAGE_SIZE=2 pattern —
  verify against how the harness injects config); fixture-direct writes confirm + explicit app
  re-sync before UI assertions; fixture guard checks tokensOfOwnerIn via publicClient, not the
  UI; anvil isolation via fork-snapshot.ts evm_snapshot/evm_revert; ~60s timeout for multi-step
  scenarios; production STREAM_PAGE_SIZE derived from the lens gas table measured in ticket 09
  (treat any gas number quoted before that measurement as placeholder).
