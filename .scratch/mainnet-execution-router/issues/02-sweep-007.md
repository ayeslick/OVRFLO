# 02 — Ignorance-lens sweep of `007` (rewritten body)

**What to do:** Run the ignorance-lens sweep on
`docs/plans/2026-08-15-007-feat-borrow-quote-by-revert-plan.md` — the **rewritten** body (direct
`previewBorrow` under via-IR). Output: point-fixes applied in place plus a `### Sweep Contracts`
section. This is the only ticket class allowed to edit the plan file.

**Repo:** `/Users/jay/OVRFLO`, branch `feat/008-mainnet-campaign`.

**Blocked by:** — | **Status:** cancelled — sweep track retired 2026-08-15 by user directive (no convergence gate); collected lens findings folded into implementation dispatch prompts | **Labels:** ready-for-agent

**Pinned models:** lens-agents `gemini-3.7-flash-high` (3 lenses, parallel), completeness critic
`cursor-grok-4.6-xhigh`. Orchestrator folds findings into the plan.

## Sweep contract (from `docs/solutions/patterns/ignorance-lens-sweep.md`)

- Lenses for this plan: (a) **implementer** — where would two onboarded agents diverge building
  the commit-flag threading, the safety-net gates, or the frontend async quote; (b) **verifier** —
  which claims lack a failing test or a pasted measurement; (c) **consumer** — does the frontend
  half (BorrowFlow, quote.ts, protocol client) have enough pinned decisions to build without
  inventing.
- Each lens verifies against source (`src/OVRFLOLending.sol`, `.scratch/preview-probe/` diff,
  `web/components/borrow/`). No speculation. Cap 6–8 findings per lens, ranked.
- A finding names: decision point, how two agents diverge, the cost, one closing sentence.
- **Quote-by-revert is out of scope by decree.** A lens that "discovers" it as missing work is
  wrong — it is the recorded fallback at `5213e59`, not work. Same for `ABI_VERSION` bumps and
  `BelowMinAcceptable` changes.
- Critic names missing lenses, runs the top one, gives the diminishing-returns verdict.
- Router review questions apply: does Solidity already own this? protocol truth, analytics, or
  convenience? does current Solidity still hold the assumption?

## Acceptance criteria

- [ ] Three lens reports + critic verdict returned to orchestrator
- [ ] Point-fixes applied in the plan in place; rest appended as `### Sweep Contracts`
- [ ] Plan status line updated to reflect the sweep
- [ ] No finding re-opens quote-by-revert, the ABI bump, or the via-IR flip decision

## Plan unit

`007` sweep, gates ticket 08.
