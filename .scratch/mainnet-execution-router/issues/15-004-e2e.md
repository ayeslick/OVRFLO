# 15 — `004` E2E: omission, reorg, cross-source

**What to build:** The adversarial E2E suite per the reconciled `004` (ticket 06): swap-and-pop
omission coverage, mid-enumeration reorg via anvil fault injection, cross-source disappearance
(degraded Borrowed lens, never a vanished stream), pagination regression cases, and the
production `STREAM_PAGE_SIZE` derivation from measured lens cost.

**Repo:** `/Users/jay/OVRFLO`, branch `feat/008-mainnet-campaign`.

**Blocked by:** 06, 14, 10 | **Status:** cancelled — not a campaign checkbox (user 2026-08-16). E2E/reorg/omission coverage is ongoing testing, not an event to close 008. Do not dispatch. | **Labels:** ready-for-agent

**Pinned model:** `cursor-grok-4.5-high`, subagent_type `generalPurpose`

## Session prompt

```text
Implement the reconciled docs/plans/2026-08-15-004 E2E plan (post ticket 06).
Ticket: .scratch/mainnet-execution-router/issues/15-004-e2e.md
Spec: .scratch/mainnet-execution-router/spec.md.

CWD: all forge/npm/vitest/playwright/git run in the ticket worktree named at dispatch (web tests:
$WORKTREE/web, local binaries). Echo pwd + git toplevel + HEAD in the same command as the test.
Wrong-tree totals are void.

Worktree load: new worktrees have no web/node_modules, no web/.env.local, no Foundry out/.
Load those before baseline (symlink or npm ci; copy env from /Users/jay/OVRFLO/web/.env.local
without printing it; forge build). Missing deps is bootstrap, not a failed baseline. See
.scratch/mainnet-execution-router/memory/2026-08-16-worktree-baseline-bootstrap.md.

Before first write: echo branch + HEAD; confirm 06, 10, 14 resolved; E2E baseline per
docs/agents/testing.md.

Binding:
- E2E runs against the REAL vendored fork artifact (ticket 10's gate), never MockSablier.
- Fixture page size 2 so multi-page paths execute in E2E.
- Reorg: anvil snapshot/revert or anvil_reorg between page N and N+1; assert the pager discards
  and restarts from a fresh pin, previous list marked stale, never a spliced mixed-pin list.
- Swap-and-pop omission: transfer/burn between pages under an UNPINNED read would omit an id —
  assert the pinned pager does not; this is the scenario the whole campaign exists to catch.
- Cross-source: pledged stream leaves owner enumeration, appears via borrower book; owner book
  ready + borrower book failed renders degraded Borrowed lens, never a vanished stream.
- Pagination regressions at the E2E level: resume-at-first-unconsumed, empty-window-advances.
- Derive and commit production STREAM_PAGE_SIZE from measured lens gas/latency; record the
  measurement in the plan-designated place (not an edit to other plans).
- Every Given arranged outside the app write flow ends with a frontend re-sync step.

Intent record before first write. Do not edit plans other than 004's designated measurement
record. Do not push.
```

## Acceptance criteria

- [ ] All five scenario families implemented and green; totals pasted
- [ ] STREAM_PAGE_SIZE derived from measurement and committed
- [ ] Runs on the real fork artifact; deviations recorded; Final diff filled

## Deviations from the plan

## Final diff

## Plan unit

`004`, wave 4. Last gate before mainnet launch readiness.
