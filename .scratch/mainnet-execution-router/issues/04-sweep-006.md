# 04 — Ignorance-lens sweep of `006`

**What to do:** Ignorance-lens sweep of
`docs/plans/2026-08-15-006-fix-factory-canonical-stream-anchor-plan.md`. Smallest plan in the
set — two lenses suffice.

**Repo:** `/Users/jay/OVRFLO`, branch `feat/008-mainnet-campaign`.

**Blocked by:** — | **Status:** cancelled — sweep track retired 2026-08-15 by user directive (no convergence gate); collected lens findings folded into implementation dispatch prompts | **Labels:** ready-for-agent

**Pinned models:** lens-agents `gemini-3.7-flash-high` (2, parallel), critic
`cursor-grok-4.6-xhigh`. Orchestrator folds.

## Sweep contract

- Lenses: (a) **implementer** — factory-root bootstrap, discriminated-union discovery states,
  required-not-nullable config; (b) **verifier/consumer** — boot failure modes (wrong chain id,
  empty `eth_getCode(factory)`), atomic bootstrap multicall, what breaks in existing `.env.local`s.
- Queue items to fold: factory bootstrap hardening (chain id + code check, threat-model the
  poisoned anchor); signing-destination verification; atomic bootstrap discovery.
- **Settled:** factory is the only static anchor; no lens address exists; registries are
  append-only/slow-dynamic, never permanently cached.
- Cap 6–8 findings per lens. Critic verdict required.

## Acceptance criteria

- [ ] Lens reports + critic verdict returned
- [ ] Point-fixes in place; `### Sweep Contracts` appended; status line updated

## Plan unit

`006` sweep, gates ticket 13.
