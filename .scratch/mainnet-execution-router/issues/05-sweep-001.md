# 05 — Ignorance-lens sweep of `001` (post-amendment)

**What to do:** Ignorance-lens sweep of
`docs/plans/2026-08-15-001-feat-watch-enumeration-load-more-plan.md` after its 2026-08-15
amendments (useInfiniteQuery not useInfiniteReadContracts; sourceCount/renderCount/complete/
confirmedEmpty state model; source-coordinate pagination invariant).

**Repo:** `/Users/jay/OVRFLO`, branch `feat/008-mainnet-campaign`.

**Blocked by:** — | **Status:** cancelled — sweep track retired 2026-08-15 by user directive (no convergence gate); collected lens findings folded into implementation dispatch prompts | **Labels:** ready-for-agent

**Pinned models:** lens-agents `gemini-3.7-flash-high` (3, parallel), critic
`cursor-grok-4.6-xhigh`. Orchestrator folds.

## Sweep contract

- Lenses: (a) **implementer** — TanStack `useInfiniteQuery` + `loadStreamPage` boundary, the
  Borrowed/Supplied symmetric shape, LOAD MORE wiring; (b) **verifier** — the four-field state
  model's edge cases (`confirmedEmpty` requires zero unresolved failures; auto-fetch on
  all-ineligible page), consumer blast-radius list at `watch-entry.ts` etc.; (c) **consumer** —
  every downstream consumer that assumed completeness or a single `count`.
- **Settled:** no dedup (duplicate id = invariant violation → unavailable); pagination progress by
  source coordinate; `STREAM_PAGE_SIZE` owned by 004; `MAX_ENUMERATION_IDS` retires; TanStack owns
  the pager state machine; protocol client owns page/complete-set operations.
- Cap 6–8 findings per lens. Critic verdict required.

## Acceptance criteria

- [ ] Lens reports + critic verdict returned
- [ ] Point-fixes in place; `### Sweep Contracts` appended; status line updated
- [ ] No finding re-opens the count model or the pager ownership boundary

## Plan unit

`001` sweep, gates ticket 14 (with 11, 13).
