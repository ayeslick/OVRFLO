# 03 — Ignorance-lens sweep of `005`

**What to do:** Ignorance-lens sweep of `docs/plans/2026-08-15-005-feat-stream-lens-plan.md`.
Point-fixes in place plus `### Sweep Contracts` completion (the plan's existing sweep notes say
"the completeness critic has not run" — this ticket runs it).

**Repo:** `/Users/jay/OVRFLO`, branch `feat/008-mainnet-campaign`.

**Blocked by:** — | **Status:** cancelled — sweep track retired 2026-08-15 by user directive (no convergence gate); collected lens findings folded into implementation dispatch prompts | **Labels:** ready-for-agent

**Pinned models:** lens-agents `gemini-3.7-flash-high` (3, parallel), critic
`cursor-grok-4.6-xhigh`. Orchestrator folds.

## Sweep contract

- Lenses: (a) **implementer** — Solidity lens (`StreamView` struct, `streamsByIds`, try/catch,
  ownership invariant) and the bytecode-generation pipeline; (b) **verifier** — which claims lack
  tests (`ok:false` reachable only via `streamsByIds`; EIP-3860 initcode gate; drift gate on
  creation bytecode); (c) **consumer** — `useStreams`, `claim-all`, BorrowFlow complete-set
  routing on `balanceOf`.
- Existing queue item to fold: derive `STREAM_PAGE_SIZE` against production RPC class.
- **Settled, not findings:** deployless (CREATE2 flip only on probe failure — recipe in plan),
  `streamsByIds` ships, struct is full surface plus owner/withdrawable/status/ok, lens bytecode
  generated under via-IR (shipping settings), no lens address in config.
- Cap 6–8 findings per lens. Critic verdict required.

## Acceptance criteria

- [ ] Three lens reports + critic verdict returned
- [ ] Point-fixes in place; `### Sweep Contracts` completed; status line updated
- [ ] No finding re-opens deployless vs deployed or the struct scope

## Plan unit

`005` sweep, gates ticket 09.
