# 01 — Confirm the claim-all mid-queue revert banner still passes in E2E

> *This was generated by AI during triage.*

**Category:** bug (verification gap, not a confirmed regression)

**What to build:** Nothing new by default — this is a confirm-or-fix task. Dogfood matrix row #8 (F6, claim-all failure messaging) was left `Blocked (needs human verify)` in `docs/dogfood-reports/2026-07-28-c1024d9-dogfood.md` because the interactive agent-browser dogfood session couldn't arrange a mid-queue on-chain revert live. Triage found this scenario is **already written** as an automated Gherkin scenario — `web/tests/e2e/claim-all.feature`, "Error state — a contract revert fails the queue mid-flight" — with step definitions in `web/tests/e2e/steps/claim-all.ts` that force a claimed-elsewhere collision. It was not run in this session (no `MAINNET_RPC_URL` / seeded E2E environment available), so its current pass/fail status is unconfirmed, not negative.

**Current behavior:** Unknown/unconfirmed. The scenario asserts the `ClaimAllModal` caption reads `TRANSACTION FAILED — RESUME RE-CHECKS CLAIMABLES` and the `RESUME` button is enabled after a queued claim fails mid-flight. No component-level unit test covers this caption (`web/components/ClaimAllModal.tsx` has no dedicated test file) — the e2e scenario is the only coverage for this exact string.

**Desired behavior:** Bring up the seeded E2E environment (`docs/agents/testing.md`: `export MAINNET_RPC_URL=...` then `npm --prefix web run bootstrap:e2e`) and run `npm --prefix web run test:e2e`, or at minimum the `claim-all.feature` file. Confirm the "Error state — a contract revert fails the queue mid-flight" scenario passes as-is.

- If it passes: close this ticket, update the dogfood report's matrix row #8 from `Blocked` to `Pass (confirmed via automated e2e, <date>)`.
- If it fails: treat it as a real regression — diagnose per `docs/agents/testing.md`'s "before concluding a failure is real" checklist (rule out environment issues first), then fix.

**Blocked by:** None — can start immediately. Requires a paid archive RPC key for `MAINNET_RPC_URL` (see `docs/agents/testing.md`).

**Status:** ready-for-agent

- [ ] E2E environment brought up via `bootstrap:e2e` (not the manual multi-step path)
- [ ] `claim-all.feature`'s "Error state — a contract revert fails the queue mid-flight" scenario run and its outcome reported (pass or fail, with log evidence)
- [ ] If it fails, root-caused and fixed (or, if it's an environment artifact per the testing doc's known-shapes list, documented as such and re-run clean)
- [ ] Dogfood report matrix row #8 updated to reflect the confirmed outcome
- [ ] Environment torn down after (`bootstrap:local:clean`) only if this agent is the one who brought it up and nothing else is using it

**Out of scope:**
- Writing new test coverage — the scenario already exists; this is a verify-and-fix task, not a test-authoring task
- Matrix row #9 (separate ticket, `02-verify-repay-closed-loan-e2e.md`)

## Comments

**2026-07-28 (triage):** Re-triaged from dogfood matrix row #8. Redundancy check found existing coverage in `web/tests/e2e/claim-all.feature`; not run this session due to missing `MAINNET_RPC_URL`. Moved directly to `ready-for-agent` since verifying existing coverage (and fixing only if genuinely red) is fully mechanical and well-specified — no design decision involved.
