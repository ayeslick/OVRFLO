# 02 — Confirm the "repay on an externally closed loan" banner still passes in E2E

> *This was generated by AI during triage.*

**Category:** bug (verification gap, not a confirmed regression)

**What to build:** Nothing new by default — this is a confirm-or-fix task, same shape as ticket 01. Dogfood matrix row #9 (F7, repay closed-loan handling) was left `Blocked (needs human verify)` because the interactive dogfood session couldn't arrange an externally-closed loan while the Repay modal stayed mounted. Triage found this scenario is **already written** as an automated Gherkin scenario — `web/tests/e2e/repay-close.feature`, "Error state — the loan disappears while the modal is open" — with a fixture helper in `web/tests/e2e/fixtures/chain.ts` that closes the loan from outside the form. Not run this session (no seeded E2E environment available); status unconfirmed, not negative.

**Current behavior:** Unknown/unconfirmed. The scenario asserts that when a loan is fully repaid from another channel while the Repay modal is open, the UI shows the `LOAN NOT FOUND` caption rather than a stale or falsely-confirming state. A related, distinct scenario in the same file ("repay reverts if the balance is drained mid-flow") already covers the on-chain-revert case and is not in scope here.

**Desired behavior:** Bring up the seeded E2E environment per `docs/agents/testing.md` and run `repay-close.feature`. Confirm the "Error state — the loan disappears while the modal is open" scenario passes as written.

- If it passes: close this ticket, update the dogfood report's matrix row #9 from `Blocked` to `Pass (confirmed via automated e2e, <date>)`.
- If it fails: rule out environment causes first (per the testing doc's known-shapes list — e.g. this exact repo has a documented history of nonce-collision and stale-read issues around fixture-direct writes racing UI polling, see `docs/solutions/ui-bugs/repayform-loan-and-balance-reads-never-refetch-without-polling.md`), then fix if it's real.

**Blocked by:** None — can start immediately. Requires a paid archive RPC key for `MAINNET_RPC_URL`.

**Status:** ready-for-agent

- [ ] E2E environment brought up via `bootstrap:e2e`
- [ ] `repay-close.feature`'s "Error state — the loan disappears while the modal is open" scenario run and its outcome reported (pass or fail, with log evidence)
- [ ] If it fails, root-caused and fixed, or documented as an environment artifact and re-run clean
- [ ] Dogfood report matrix row #9 updated to reflect the confirmed outcome
- [ ] Environment torn down after, only if this agent brought it up and nothing else is using it

**Out of scope:**
- Writing new test coverage — the scenario already exists
- The adjacent "repay reverts if balance drained mid-flow" and "insufficient balance" scenarios in the same feature file — those are separate, already-passing-or-failing independently

## Comments

**2026-07-28 (triage):** Re-triaged from dogfood matrix row #9. Redundancy check found existing coverage in `web/tests/e2e/repay-close.feature`; not run this session due to missing `MAINNET_RPC_URL`. Same rationale as ticket 01 for going straight to `ready-for-agent`.
