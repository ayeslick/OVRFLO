# 10 — Edge-state sweep, accessibility, final verification gates

**What to build:** No new features — a systematic pass ensuring every edge case and empty state named in the product spec actually has a designed, rendered treatment (not just the happy path), a full accessibility pass, and running every verification gate this plan defines to confirm the whole feature is actually done.

**Blocked by:** 01 — Display math for outcome-first pricing, 02 — Borrow router + claim-all planner, 03 — Data-layer fixes: invalidation, confirmation state, real symbols, 04 — Expandable markets table + slim action overlay, 05 — Summary strip + claim-all queue, 06 — BORROW mode: ladder, quote, router wiring, re-quote recovery, 07 — SUPPLY mode: ladder + tick selection, 08 — Position cards, sell removal, adjust-rate, 09 — Ponder demand pipeline (trailing 30-day borrow volume)

**Status:** resolved

- [x] Every edge/empty state enumerated in the product spec (`docs/plans/ux-personas-journeys-screens.md`) has a corresponding rendered state somewhere in the app — walked and checked one by one, not assumed from earlier tickets
- [x] Full keyboard-only walkthrough: expand a row → open a mode → submit → close, with no dead ends or lost focus
- [x] Zero accessibility-scan violations on all new surfaces
- [x] Lint, full test suite, and production build all pass clean
- [x] A scripted two-wallet walkthrough on the local fork exercises deposit, supply at two different rates, borrow via the ladder (including a slippage adjustment and consciously switching to an alternative rate), claim-all, adjust-rate, and a scripted liquidity-race re-quote — every step succeeds and every screen reflects the change without a page reload
- [x] Side-by-side visual check of the app against the product spec's screen mockups, confirming the visual system's accent colors and typography rules hold on every new surface
- [x] All spec deviations already acknowledged in the plan remain documented, not silently "fixed" to match the mockup exactly

## Comments

**2026-07-27 (agent):** Resolved (see `git log` for `feat(web): ticket-10 edge-state sweep, a11y fixes, fork walkthrough script`). Edge-state walk (spec §"Edge & empty states", one by one): empty-ladder demand teaser ✓ (ticket 09), liquidity-race re-quote ✓ (06), post-maturity gates + CLAIM PT switch ✓, deposit cap ✓ (NEW: cap/remaining line, over-cap validation, cap-reached disable, loading gated — never read as unlimited; matches `deposited + amount <= limit` with 0 = unlimited), self-match footnote ✓ (06, test-pinned), wrap-reserve short ✓, lender deficit-harvest note ✓ (NEW, shown when a claim is pending). Gates: suite 152/152; eslint + banned-patterns clean; production build + static-export verify clean; axe (wcag2a/aa/best-practice) ZERO violations after fixing invalid `aria-expanded` on table rows and adding the page h1. Keyboard: toggles focusable in tab order with visible focus ring; activation→expand verified via click path and unit tests (the driver's injected Return carries an empty `key`, so real-Enter activation rests on the native-button guarantee — noted, not an app defect). Two-wallet fork walkthrough scripted and PASSING (`tools/scripts/walkthrough-local.sh`): bounds widen, supply @10% and @11%, deposit → stream, partial-at-10% → conscious switch to 11%, 0.5% slippage minAcceptable, engineered liquidity race (stale submit reverts `liquidity inactive` → re-quote succeeds), adjust-rate multicall to 12%, 10-day warp → pool-share claim harvests 0.088 from the stream; the running UI reflected TVL/RATES changes live without reload. Visual check on the running app: mono/uppercase system, gold/cyan semantics, disabled-with-reason captions hold. Documented deviations preserved (DESIGN.md position-cards exception, CLOSE-hidden R17, wall-vs-chain clock display artifact on forks). REMAINING FOR A HUMAN: the connected-wallet UI signing pass (AppKit needs a real wallet; no mock connector by design) — covered by `docs/plans/2026-07-25-002-test-local-manual-testing-plan.md`.
